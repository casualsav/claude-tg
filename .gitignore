node_modules/
.env
*.pid
*.sock
pending-events.jsonl
inbox/
*.corrupt-*
__pycache__/

# graphify analysis output
graphify-out/
*.log
*.bak
.DS_Store

# local Claude Code project settings (never ship operator-specific config)
.claude/settings.local.json

# session handoff baton — a local, ephemeral working note (/handoff writes it,
# /continue consumes + clears it); never commit it or it goes stale in the repo
handoff.md
